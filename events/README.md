# Events

Copy `_template` to a unique lowercase event ID. Do not edit `_template` for event-specific details. Final event Markdown records are intended for Git; runtime sandboxes and source checkouts are not.

An event progresses through configuration, intake, evidence, initial judging, consolidation, bracket, tournament, dossiers, final audit, and complete. `status.md` is the authoritative progress ledger.
