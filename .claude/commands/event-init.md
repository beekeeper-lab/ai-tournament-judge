---
description: Initialize and validate a new judging event directory.
argument-hint: <event-directory>
---

Use the `run-judging-event` skill in initialization mode for `$ARGUMENTS`. Validate configuration, create missing event subdirectories from `events/_template`, set the event status to intake, and report blockers. Do not begin judging.
