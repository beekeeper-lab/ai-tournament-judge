---
description: Safely ingest one team and prepare its evidence package.
argument-hint: <event-directory> <team-id>
---

Use the `prepare-submission` skill for `$ARGUMENTS`. Pin the source commit, apply the execution-safety policy, generate the evidence manifest, validate it, and update status. Stop on missing authorization or unsafe execution requirements.
